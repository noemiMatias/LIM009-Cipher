window.cipher = {
  encode: (offset,texto-cifrar) => {
    /* Acá va tu código */
    let nuevoMensaje=" ";
    for(i =0;i<=texto-cifrar.length;i++){
      if(texto-cifrar[i])
    
    if(i=65<=90);
    console.log(encode);

  },
  decode: () => {
    /* Acá va tu código */
  }
};
